{% extends 'base.html.twig' %}

{% block title %}Crear Pelicula{% endblock %}

{% block body %}

    <div class="example-wrapper">
    
        <div class="container bg-dark text-white mt-2">
            <div class="mb-3">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}